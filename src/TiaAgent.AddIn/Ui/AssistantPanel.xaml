<UserControl x:Class="TiaAgent.AddIn.Ui.AssistantPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TiaAgent.AddIn.Ui">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="10">
            <DockPanel>
                <TextBlock Text="{Binding Title}"
                           Foreground="White"
                           FontSize="14"
                           FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="10">
            <TextBox Text="{Binding Content, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     BorderThickness="0"
                     Background="Transparent"
                     FontFamily="Consolas"
                     FontSize="12"/>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="6,4" BorderBrush="#D0D0D0" BorderThickness="0,1,0,0">
            <DockPanel>
                <!-- Loading indicator -->
                <ProgressBar IsIndeterminate="{Binding IsBusy}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
                             Width="100"
                             Height="16"
                             Margin="0,0,8,0"
                             DockPanel.Dock="Left"/>
                <TextBlock Text="{Binding Status}"
                           Foreground="Gray"
                           FontSize="11"
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
